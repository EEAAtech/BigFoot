@model BigFoot.Menus

@{
    ViewBag.Title = "Delete";
}

<h2>Delete (First ensure ALL submenus that are under this Menu item are deleted)</h2>

<h3>Are you sure you want to delete this?</h3>
    <p>Note: This action does NOT delete any underlying pages</p>
<div>
    <h4>Menus</h4>
    <hr />
    <dl class="dl-horizontal">
        <dt>
            @Html.DisplayNameFor(model => model.Menu)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.Menu)
        </dd>

        <dt>
            URL
        </dt>

        <dd>
            @Html.DisplayFor(model => model.mUrl)
        </dd>

    </dl>

    @using (Html.BeginForm()) {
        @Html.AntiForgeryToken()

        <div class="form-actions no-color">
            <input type="submit" value="Delete" class="btn btn-default" /> |
            @Html.ActionLink("Back to List", "Index")
        </div>
    }
</div>
